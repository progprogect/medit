# Gemini API key (required for video analysis)
# Locally: set your key here or in environment
# On AWS: will be loaded from Secrets Manager when AWS_SECRETS_ENABLED=true
GEMINI_API_KEY=

# Storage mode: "local" for dev without AWS, "s3" for production
STORAGE_MODE=local

# Local storage paths (used when STORAGE_MODE=local)
UPLOAD_DIR=./uploads
OUTPUT_DIR=./outputs

# Pexels API key (optional — needed for fetch_stock_video / fetch_stock_image tasks)
# Get free key at https://www.pexels.com/api/
PEXELS_API_KEY=

# AWS (optional, for S3 mode)
# AWS_REGION=
# S3_BUCKET=
